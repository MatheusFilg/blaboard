model Organization {
  id        String   @id @map("_id")
  name      String
  slug      String   @unique
  logo      String?
  metadata  Json?
  createdAt DateTime @default(now())

  columns Column[]
  tasks   Task[]
  labels  Label[]
  milestones Milestone[]

  @@map("organization")
}
